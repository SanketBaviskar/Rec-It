import React from 'react';

const DefaultView = () => {
  return (
    <div>
      After clicking options from the sidebar, we will show different components here.
    </div>
  );
};

export default DefaultView;